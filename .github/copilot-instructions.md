When performing a code review, apply the checks in the prompts/review-code.prompt.md file.

When performing a code review, focus on verifier correctness, EIP-712 binding, fund safety in smart wallets, proxy/factory integrity, and readability. Avoid nested ternary operators.
